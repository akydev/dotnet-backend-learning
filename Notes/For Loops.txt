Used when you know how many times you want to repeat something.

Syntax:

for (int i = 0; i < 5; i++)
{
    Console.WriteLine(i);
}

How it works:

int i = 0 → initialization (runs once)

i < 5 → condition (checked every time)

i++ → increment (after every iteration)

